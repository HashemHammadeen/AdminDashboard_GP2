<% content_for(:page_title) { "Generate QR Code" } %>
<% content_for(:page_subtitle) { "Create a new QR code for your shop" } %>

<div class="max-w-2xl bg-white border border-gray-200 rounded-xl shadow-sm p-6">
  <%= render "form", qr: @qr %>
</div>
