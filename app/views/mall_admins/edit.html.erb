<% content_for(:page_title) { "Edit Mall Admin" } %>
<% content_for(:page_subtitle) { "Update details or rotate credentials for #{@mall_admin.name}" } %>

<div class="max-w-2xl bg-white border border-gray-200 rounded-xl shadow-sm p-6">
  <%= render "form", mall_admin: @mall_admin %>
</div>
